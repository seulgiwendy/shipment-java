package model.carriage;

import model.location.StopPoint;
import model.time.Clock;

public class Ship {

    private Clock clock;
    private ContainerDeck deck;

    public void stopAtPoint(StopPoint point) {

    }
}
